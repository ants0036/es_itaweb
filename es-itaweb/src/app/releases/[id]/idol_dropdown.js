"use client"

// try to code this later 

export default function IdolDropdown({ names }) {

    /*var dropdown = document.getElementById("IdolDropdown");
    for (let i = 0; i < names.length ; i++) {
        console.log(names[i])
        var option = document.createElement("option");
        option.text = names[i]
        dropdown.add(option)
    }
    return (
        <select id = "IdolDropdown"> </select>
    )*/
    //const Dropdown = ({ names }) => {
     //   console.log("dsfdsfds")
     /*
        return (
            <div>
                <label >Assign user(s) to as task:</label>
                <button>
                        <div >
                            <ul >
                                {names.map((val, key) => (
                                    <li
                                        key={key}>
                                        <span>{val}</span>
                                    </li>
                                ))}
                            </ul>
                        </div>
                </button>
            </div>
        );
    //}*/
}